name = "schengen-visa-support"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# Note: Redirects and headers should be configured in _redirects and _headers files
# for Cloudflare Pages, not in wrangler.toml